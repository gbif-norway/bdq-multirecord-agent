# BDQ API
BDQ_API_BASE=https://bdq-api-638241344017.europe-west1.run.app

# Gmail via IMAP/SMTP (use an app password)
IMAP_HOST=imap.gmail.com
IMAP_USER=your.address@gmail.com
IMAP_PASSWORD=app-password-here
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=${IMAP_USER}
SMTP_PASSWORD=${IMAP_PASSWORD}

# Polling filters
GMAIL_POLL_LABEL=DwC-Incoming
GMAIL_FROM_FILTER=
GMAIL_PROCESSED_LABEL=DwC-Processed

# Service
MAX_WORKERS=8
HTTP_TIMEOUT_S=30
DISCORD_WEBHOOK_URL=
REPLY_FROM=
MAX_ATTACHMENT_MB=25
PORT=8080
